# v0.4.6-beta.3

- Fix the logging failure-path regression in beta.2 test coverage so release builds validate the intended `See log:` behavior.
- Keep the per-invocation logging and node plugin traceability changes from beta.2 intact.
