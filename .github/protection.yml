branch: main
ruleset:
  name: main-build-gate
  enforcement: active
  pull_request:
    required: true
  deletion: false
  force_push: false
  required_checks:
    - guard (ubuntu-latest)
    - guard (windows-latest)
    - guard (macos-latest)
